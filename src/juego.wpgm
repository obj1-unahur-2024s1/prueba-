import wollok.game.*
import tanque.*
import configuraciones.*
import clases.*

program juego {
	game.title("WAR TANK")
	game.addVisual(tanque)
	game.width(15)
	game.height(15)
	game.cellSize(32)
	configuracion.teclado()
	game.start()
}